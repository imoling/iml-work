package com.imlwork.admin.repository;

import com.imlwork.admin.model.SandboxConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SandboxConfigRepository extends JpaRepository<SandboxConfig, Long> {
}
